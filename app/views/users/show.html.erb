<section class="user-section">
  <h2><%= "Name: #{@user.name}" %></h2>

    <article class="timeline">
     
 <% unless friend_or_friendship_requested(@user, current_user) %>
  <%= link_to 'Add Friend', friendships_path(user_id: @user.id), method: :post %>
 <% end %> 
   
    <h3>Recent posts:</h3>
    <ul class="posts">
      <%= render @tweets %>
    </ul>
  </article>
</section>
